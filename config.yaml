# jorge-cron-jobs 配置文件 (Hermes 兼容版)
# 复制为 config.yaml 后填入真实值

# 邮件配置（QQ邮箱SMTP）
mail:
  smtp_server: "smtp.qq.com"
  smtp_port: 465
  smtp_user: "569545015@qq.com"
  # ⚠️ 请填入QQ邮箱授权码（不是登录密码）
  smtp_pass: "iylylmwnitbbbebi"
  to_email: "569545015@qq.com"

# Hermes 配置
hermes:
  model: "kimi-coding"

# 路径配置
paths:
  workspace: "/home/ubuntu/.hermes"
  logs: "/home/ubuntu/.hermes/cron/logs"
  scripts: "/home/ubuntu/.hermes/cron/scripts"
